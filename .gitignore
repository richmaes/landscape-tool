# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Build + render output
out/
*.tmp

# Editor autosave sidecars (src/landscape/editor_session.py)
*.autosave

# OS
.DS_Store

# Local 3D models (TurboSquid etc.): licences don't allow redistributing the
# files, so nothing in models/ is committed except its README.
models/*
!models/README.md
